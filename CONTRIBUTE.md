# Contributing

## Publishing to PyPI

1. Bump the version in `pyproject.toml`
2. Build: `uv build`
3. Publish: `uv publish --token pypi-YOUR_TOKEN_HERE`

Get a token at https://pypi.org/manage/account/token
